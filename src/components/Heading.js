import '../styles/Heading.css';

const Heading = () => {
    console.count("Heading: ");
    return (
        <header>
            Contact Manager
        </header>
    );
}
export default Heading;